{
  "name": "@altocomputer/notes-exporter-obsidian",
  "version": "0.3.0",
  "description": "Sync Apple Notes, Bear, and Logseq into your vault as plain Markdown — sync status, native app icons, and deep links back to the source app.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@codemirror/state": "^6.7.1",
    "@codemirror/view": "^6.43.6"
  }
}
